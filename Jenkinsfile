pipeline {
    ageny ageny
    stages {
        stage (git checkout) {
            steps {
                git 'https://github.com/venky0349/java-project.git'
            }
        }
        stage (git checkout) {
            steps {
                
            }
        }
    
    }
}